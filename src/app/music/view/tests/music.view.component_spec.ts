describe('Music view component', () => {
    describe('constructor', () => {
        it('adds a new svg icon', () => {

        });
    });

    describe('ngOnInit', () => {
        let musicService = jasmine.createSpyObj('musicService', ['trackAdded', 'trackRemoved']);

        it('sets volume to 0', () => {

        });
    });

    describe('removeTrack()', () => {
        it('pauses and removes the track', () => {

        });
    });

    describe('changeSongState()', () => {
        it('starts the song when it was paused', () => {

        });

        it('pauses the song when it was playing', () => {

        });
    });

    describe('changeName()', () => {
        it('opens a dialog', () => {

        });
    });
});